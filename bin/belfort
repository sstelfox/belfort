#!/usr/bin/env ruby

require 'English'

belfort_path = File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib'))
$LOAD_PATH.unshift(belfort_path) unless $LOAD_PATH.include?(belfort_path)

require 'belfort'


